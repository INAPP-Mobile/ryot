{
  "POSTGRES_USER": {
    "value": "ryot",
    "description": "PostgreSQL superuser username."
  },
  "POSTGRES_PASSWORD": {
    "value": "${{POSTGRES_PASSWORD}}",
    "description": "PostgreSQL superuser password. Set via deploy form — must be non-empty."
  },
  "POSTGRES_DB": {
    "value": "ryot",
    "description": "PostgreSQL database name."
  },
  "DATABASE_URL": {
    "value": "postgresql://${{POSTGRES_USER}}:${{POSTGRES_PASSWORD}}@${{PRIVATE_DOMAIN}}:5432/${{POSTGRES_DB}}",
    "description": "PostgreSQL connection string constructed from auto-generated credentials."
  },
  "PGDATA": {
    "value": "/var/lib/postgresql/data/pgdata",
    "description": "PostgreSQL data directory under the volume mount. Required by Railway Postgres image."
  }
}
